staffchat:
  show-in-console: true

vanish:
  disable-item-pickup: true

staffmode:
  game-mode: SPECTATOR
  enable-vanish: true
  disable-vanish-on-exit: true

moderation:
  # Keeps recent sanctions per player without letting the file grow forever.
  max-history-per-player: 100
