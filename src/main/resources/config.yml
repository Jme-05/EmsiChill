language: es
prefix: "&8[&5EmsiChill&8] "

modules:
  homes: true
  authentication: true
  skins: true
  teleport: true
  player-info: true
  staff: true
  regions: true
  graves: true
  social: true

audit-log:
  enabled: false

updates:
  enabled: true
  repository: Jme-05/EmsiChill
  timeout-seconds: 6
  ignored-version: ""
  staged-version: ""
  automatic:
    enabled: true
    interval-minutes: 30
    check-on-admin-join: true
    join-cache-max-age-seconds: 60
    notify-console: true
    notify-admins: true
  install:
    # Mantén la instalación apagada salvo que confíes plenamente en el repositorio configurado.
    enabled: false
    # El feed solo sirve para avisos; instalar requiere tamaño y SHA-256 de la API de GitHub.
    allow-feed-fallback: false
    timeout-seconds: 60
    max-download-megabytes: 25
